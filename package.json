{
	"name": "@brandsafari/attributes-fixed-tab-height",
	"version": "1.0.0",
	"description": "Fixed the tab height of Webflow tabs, so that the content is not jumping when switching tabs.",
	"keywords": [],
	"author": {
		"name": "Brandsafari"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/brandsafari/developer-starter.git"
	},
	"type": "module",
	"main": "src/index.js",
	"module": "src/index.js",
	"files": [
		"dist"
	],
	"scripts": {
		"dev": "cross-env NODE_ENV=development node ./bin/build.js",
		"build": "cross-env NODE_ENV=production node ./bin/build.js",
		"format": "prettier --write ./src",
		"release": "changeset publish",
		"update": "npm update -i -L -r"
	},
	"devDependencies": {
		"cross-env": "^7.0.3",
		"esbuild": "^0.17.7",
		"prettier": "^2.8.4"
	},
	"dependencies": {
		"@finsweet/ts-utils": "^0.37.3"
	}
}